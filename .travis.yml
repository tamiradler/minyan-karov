matrix:
  include:
    - language: java
      jdk: oraclejdk8
      before_script:
        - cd srv
      script:
        - mvn clean install

    - language: node_js
      before_script:
        - cd clt
      script:
        - npm install